# Django
SERVICE_VARIANT=lms
DJANGO_SETTINGS_MODULE=lms.envs.fun.docker_run
ALLOWED_HOSTS=["*"]

# Database
MYSQL_ROOT_PASSWORD=
MYSQL_ALLOW_EMPTY_PASSWORD=yes
MYSQL_DATABASE=edxapp
MYSQL_USER=edxapp_user
MYSQL_PASSWORD=password

# Email
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=mailcatcher
EMAIL_PORT=1025

# Python
PYTHONUNBUFFERED=1

# Features
FEATURES={"AUTOMATIC_AUTH_FOR_TESTING": true, "RESTRICT_AUTOMATIC_AUTH": false, "ENABLE_OAUTH2_PROVIDER": true, "ENABLE_CORS_HEADERS": true, "ENABLE_CROSS_DOMAIN_CSRF_COOKIE": true}
OAUTH_ENFORCE_SECURE=
OAUTH_ENFORCE_CLIENT_SECURE=
OAUTH_OIDC_ISSUER=http://edx:8073/oauth2

# CORS
CORS_ALLOW_CREDENTIALS=true
CORS_ALLOW_INSECURE=true
CROSS_DOMAIN_CSRF_COOKIE_DOMAIN=.local.dev
CROSS_DOMAIN_CSRF_COOKIE_NAME=edx_csrf_token
SESSION_COOKIE_NAME=edxcsrftoken
# Do not enable CORS_ORIGIN_ALLOW_ALL for production purpose!
# Instead set CORS_ORIGIN_WHITELIST properly
CORS_ORIGIN_ALLOW_ALL=true

# API
EDX_API_KEY=FakeEdXAPIKey

# Richie synchronization
COURSE_HOOKS=[{"url": "https://richie:8070/api/v1.0/course-runs-sync/", "secret": "shared secret", "verify": false}]
